    /*
     * Convert a redundant "idempotent" store to a tracked load.
     * This helps minimize the wrSet size and reduces false+ aborts.
     * Conceptually, "a = x" is equivalent to "if (a != x) a = x"
     * This is entirely optional
     */


check bit patterns and change local hash function ( bloom filter? )
resize local hash maps ( # writes, # conflicts )
write-back to write-through ( # aborts vs. # commits )
contention strategies (priorities, lock stealing)

kick wbloom?